import { Color } from '@pillar-ui/core'

export interface JobOptionProps {
  title: string
  number: number
}

export interface JobStaticArticleProps {
  city: string
  number: number
  color: Color
}
